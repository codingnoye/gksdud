# 기여 안내

## 작업 흐름

1. 작업 목적과 범위를 정합니다. 이슈를 만들 필요는 없습니다.
2. 최신 `main`에서 `feat/설명`, `fix/설명`, `chore/설명` 형식의 브랜치를 만듭니다.
3. 변경과 검증을 작업 브랜치에 커밋하고 PR을 엽니다. 제목은 `[FEAT] 기능 추가`, `[FIX] 버그 수정`, `[CHORE] 유지관리`처럼 작성합니다.
4. 자동 검사를 통과하고 대화를 해결한 뒤 **Squash and merge**로 병합합니다. PR 제목은 main에 남을 커밋 제목이므로 변경 내용을 간결하게 적습니다.
5. 병합한 브랜치는 삭제하고 다음 작업은 최신 `main`에서 시작합니다.

`main`에 직접 push하거나 공개한 릴리스 태그를 이동하지 않습니다. 단독 유지관리 프로젝트이므로 타인의 승인을 필수로 요구하지 않지만, PR과 자동 검사는 생략하지 않습니다.

PR 검사는 ad-hoc 서명을 사용하며 배포 서명 키나 Secrets가 필요하지 않습니다.

PR 제목에는 `[FEAT]`, `[FIX]`, `[CHORE]`, `[DOCS]`, `[REFACTOR]`, `[TEST]`, `[CI]`, `[PERF]`, `[BUILD]`, `[REVERT]` 중 하나를 사용합니다. 이슈 연결은 필수가 아니며, 에이전트는 사용자 요청 없이 이슈를 만들지 않습니다.

## 개발 환경과 빌드

Xcode Command Line Tools가 설치된 macOS에서 빌드합니다. 외부 패키지는 필요하지 않습니다.

```sh
GKSDUD_SIGN_MODE=ad-hoc bash build.sh
```

빌드 과정에서 자체 테스트를 실행하고 `outputs/`에 Universal ZIP을 만듭니다. ad-hoc 개발 빌드로 기존 설치본을 교체하면 접근성 권한을 다시 허용해야 할 수 있습니다.

## 검증

자동 테스트는 매핑 보존, 복구, 전환 이벤트, 길게 누르기, 대소문자 보존 상태 처리를 검사합니다. 실제 입력 조합, 다른 Mac에서의 최초 설치, Intel 기기, 로그인 및 잠자기 복귀는 별도의 수동 검증이 필요합니다.

### 키보드별 설정과 자동 복구 검증

- 자체 테스트는 가짜 장치 공급자를 사용해 실행 후 장치 추가, 연결 ID 교체, 처리 도중 제거, 기본값과 On/Default/Off 우선순위, 연결 해제·앱 재시작 후 설정 보존, 일부 장치 실패, 3회 연속 실패 경고와 복구, 쓰기 후 검증 실패 시 복원을 검사합니다. 실제 HID 매핑을 변경하지 않습니다.
- 단축키 저장소를 대체한 테스트로 macOS 함수키 플래그 정규화 후 복원, 비활성·미설정 원본, 사용자 변경 보존, 대상 키 변경, 기존 버전 백업 및 복원 재시도를 확인합니다. 실제 시스템 단축키와 `activateSettings`는 호출하지 않습니다.
- 빌드한 실행 파일의 `--render-keyboard-ui /private/tmp/gksdud-ui`는 가짜 장치로 설정 창과 목록을 밝은/어두운 모드 PNG로 저장하고, 스위치·세그먼트 액션과 경고 해제를 검사합니다. 테스트 창을 잠깐 표시하며 실제 HID 매핑이나 시스템 단축키는 변경하지 않습니다.
- 실제 입력 검증에서는 Karabiner 가상 키보드 재생성, USB/Bluetooth 재연결, 로그인 및 잠자기 복귀 후 자동 적용을 확인합니다. Karabiner의 외부 키보드 연결 시 내장 키보드 비활성화도 유지되어야 합니다.
- 기본값 Off인 상태에서 새 키보드가 적용되지 않는지, 개별 On은 적용되는지 확인합니다. 개별 Off는 gksdud가 적용한 매핑만 원복하며 원래 다른 앱이 설정한 매핑은 보존해야 합니다.
- 연결 해제한 키보드는 회색 이름으로 남고 선택을 바꿀 수 있어야 합니다. 재연결 시 저장한 선택이 적용되어야 합니다.
- 시리얼 또는 물리 고유 ID가 있으면 재연결과 포트 변경에도 같은 설정을 사용합니다. 고유 ID가 없는 장치는 연결 위치로 구분하고, 위치도 없는 동일 모델은 하나의 설정을 공유합니다. Karabiner 가상 키보드의 버전 문자열은 모델 식별에서 제외합니다.
- 3회 연속 실패 후 메뉴바 아이콘 좌하단과 활성화 아래에 주황색 안내가 표시되고, 성공 또는 연결 해제 후 없어지는지 확인합니다. 단일 실패로 팝업이 나타나면 안 됩니다.
- 키보드 선택은 macOS에 노출되는 HID 서비스 기준입니다. Karabiner가 여러 물리 키보드의 입력을 가상 키보드 하나로 합치는 경우 가상 키보드의 선택이 해당 입력에 적용됩니다.

## 릴리즈 노트

`.github/RELEASE_NOTES.md`는 릴리즈 초안 생성에 쓰는 공통 설치·업데이트 안내입니다. 버전별 변경 내용은 이 템플릿에 넣지 않습니다.

릴리즈 워크플로가 초안을 만들면 GitHub Releases에서 본문 상단에 해당 버전의 변경 내용을 작성하고 확인한 뒤 게시합니다. 게시한 릴리즈의 설명도 GitHub Releases에서 직접 수정합니다. 템플릿 수정은 이미 생성된 릴리즈 본문에 반영되지 않습니다.

`CHANGELOG.md`에는 저장소의 버전별 변경 이력을 유지합니다.

## 관련 문서

- [변경 기록](CHANGELOG.md)

### 대소문자 보존 수동 검증

- `누를 때 전환`을 켠 경우와 끈 경우 모두, 영문 대문자 → 한글 → 영문이 대문자로 돌아오는지 확인합니다. 소문자도 같은 방식으로 확인합니다.
- `길게 눌러 대소문자 전환`과 보존 옵션의 네 가지 조합을 확인합니다. 보존을 켜면 짧게 누르거나 메뉴에서 언어를 바꿔도 영문 대소문자가 유지되어야 합니다. 두 옵션을 모두 켜면 한글 상태에서도 다시 길게 누를 때 기억한 상태의 반대로 전환되어야 합니다.
- 길게 누르기를 켜고 보존을 끄면 현재 Caps Lock 상태를 기준으로 길게 누르기가 동작하고, 한영 전환 후 이전 대소문자를 복원하지 않아야 합니다. 두 옵션 모두 서로의 체크 상태를 바꾸지 않아야 합니다.
- 길게 누르기를 켜거나 끈 경우 모두 영문 상태에서 일반 Caps Lock으로 대소문자가 바뀌어야 합니다. 보존이 켜져 있으면 일반 Caps Lock으로 바꾼 상태가 한영 왕복 후에도 유지되고, 다음 길게 누르기는 그 상태의 반대로 전환되어야 합니다. 보존까지 끄면 앱이 Caps Lock을 복원하지 않아야 합니다.
- 빠른 연속 전환 직후 첫 영문 글자, 앱 전환, 잠자기 복귀, 접근성 권한 해제 후 재허용도 확인합니다. 자체 테스트 통과만으로 실제 입력기 타이밍을 검증한 것으로 간주하지 않습니다.
